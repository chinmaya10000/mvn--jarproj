pipeline {
  agent any
  stages {
    stage("compile") {
      step {
        echo 'compiling the application'
      }
  }
   stage("test") {
      step {
        echo 'testing the application'
      }
  }
    stage("package") {
      step {
        echo 'packaginging the application'
      }
  }
}
